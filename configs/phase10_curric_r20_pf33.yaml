# Step 2 §10.1 — base curriculum (final 1:20), positive_fraction 0.33 (fewer positives → toward deployment prevalence).
base: phase0c_seed42.yaml
data:
  data_root: /outputs/v1.0/data_local
  normalization_stats_path: /outputs/v1.0/staging/v1_splits/normalization_stats.json
channels: {rgb: true, extra: []}
loss: {boundary_handling: ignore, boundary_ignore_width: 2}
sampling:
  positive_fraction: 0.33
mlflow:
  run_name: phase10_curric_r20_pf33
